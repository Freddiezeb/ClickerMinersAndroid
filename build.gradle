// GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN
buildscript {
	repositories {
		jcenter()
		google()
		maven {
			url "http://repo1.maven.org/maven2"
		}
	}

	dependencies {
		classpath 'com.android.tools.build:gradle:3.0.1'
		classpath 'com.neenbedankt.gradle.plugins:android-apt:1.8'
//        classpath 'me.tatarka:gradle-retrolambda:3.4.0'
	}
}

repositories {
	maven {
		url "http://repo1.maven.org/maven2"
	}
}

allprojects {
	repositories {
		flatDir {
			dirs 'libs'
		}
		google()
	}
}

apply plugin: 'com.android.application'
//apply plugin: 'me.tatarka.retrolambda'

dependencies {
	implementation 'com.android.support.constraint:constraint-layout:1.1.0'
	compile fileTree(include: ['*.jar'], dir: 'libs')
	implementation 'com.jakewharton:butterknife:8.5.1'
	annotationProcessor 'com.jakewharton:butterknife-compiler:8.5.1'
}

android {
	compileSdkVersion 26
	buildToolsVersion '27.0.1'

	defaultConfig {
		targetSdkVersion 26
		applicationId 'com.owlen.clickerminersandroid'
	}

	lintOptions {
		abortOnError false
	}

	aaptOptions {
		noCompress '.unity3d', '.ress', '.resource', '.obb'
	}

	signingConfigs {
		release {
			storeFile file('C:/Users/Freddie/Documents/GitHub/ClickerMiners/ClickerMinersAndroid/clickerminers.keystore')
			storePassword 'clickerminers'
			keyAlias 'clickerminers'
			keyPassword 'clickerminers'
		}
	}

	buildTypes {
		debug {
			minifyEnabled false
			useProguard false
			proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
			jniDebuggable true
		}
		release {
			minifyEnabled false
			useProguard false
			proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
			signingConfig signingConfigs.release
		}
	}

	compileOptions {
		sourceCompatibility JavaVersion.VERSION_1_8
		targetCompatibility JavaVersion.VERSION_1_8
	}
}
